# riscv-mini-os

A small RISC-V kernel that boots on QEMU `virt`, runs user processes from a
custom on-disk filesystem (KTFS), and supports `fork`/`exec`/`wait` with Sv39
paging.

> Repository scaffolding in progress. Architecture overview, build
> instructions, and demo will land here as the migration proceeds.

License: [NCSA](LICENSE) — see [NOTICE](NOTICE) and [AUTHORS](AUTHORS).
